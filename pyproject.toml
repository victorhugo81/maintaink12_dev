[project]
name = "maintaink12"
version = "0.11.0"
description = "K-12 CMMS / work-order management for district M&O departments, built on the AssistItK12 codebase"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=50.0.0",
    "dotenv>=0.9.9",
    "email-validator>=2.3.0",
    "flask>=3.1.2",
    "flask-caching>=2.3.1",
    "flask-mail>=0.10.0",
    "flask-login>=0.6.3",
    "flask-migrate>=4.1.0",
    "flask-paginate>=2024.4.12",
    "flask-wtf>=1.2.2",
    "pymysql>=1.1.2",
    "flask-limiter>=4.1.1",
    "flask-apscheduler>=1.13.1",
    "idna>=3.15",
    "qrcode[pil]>=8.0",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.1",
    "pytest>=9.0.3",
    "pytest-flask>=1.3.0",
]

[tool.pytest.ini_options]
filterwarnings = [
    # Expected in test environment — rate limiter uses in-memory storage by design
    "ignore:Rate limiter is using in-memory storage:RuntimeWarning",
    # SQLAlchemy legacy Query.get() shim inside flask-sqlalchemy — not our code
    "ignore:.*Query.get\\(\\) method is considered legacy.*:sqlalchemy.exc.LegacyAPIWarning",
]
